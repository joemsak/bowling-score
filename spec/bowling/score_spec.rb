require 'spec_helper'

describe Bowling::Score do
  it 'has a version number' do
    expect(Bowling::Score::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
